class Solution {
    public boolean isValid(String s) {
        Deque<int> stack = new ArrayDeque<>();
        int length = s.length();
        for(int i = 0;i<length;i++){
            if('('){
                
                                                                
            }
        }
        
    }
}
